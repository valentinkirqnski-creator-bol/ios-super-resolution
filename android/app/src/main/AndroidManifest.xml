<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Not required: the app still merges DNG files on a device whose camera
         cannot emit RAW, and refusing to install there would be worse than
         saying so on the first screen. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.raw" android:required="false" />

    <application
        android:label="FuzeFrame"
        android:icon="@android:drawable/ic_menu_camera"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:largeHeap="true"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
